<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, label: 'Your Review', error: 'Review is mandatory, please add one' %>
  <%= f.input :rating, label: 'Rating' %>
  <%= f.button :submit %>
<% end %>
